# Downloaded runtimes + installed tools + vendored config are build artifacts, not source.
/dist/
/bin/
/tools/
/staging/
config/.claude/
# Never commit secrets — ignore any .secrets/ folder anywhere in the tree
**/.secrets/
# Skills are vendored at build time, not tracked (for now)
**/skills/
*.log
.DS_Store
